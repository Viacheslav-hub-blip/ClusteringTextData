"""Prompt templates for the agentic banking clustering project."""

DOMAIN_CONTEXT = """
Это комментарии пользователей о банковском сервисе и связанных сценариях:
- переводы
- платежи и оплата услуг
- подтверждение операций
- пуши, коды, уведомления
- проверки, антифрод, блокировки
- мобильное приложение, личный кабинет, поддержка
- удобство, прозрачность, скорость, безопасность

Трактуй слова в банковом и сервисном контексте, если текст явно не указывает иное.
Избегай нерелевантных доменов вроде медицины, логистики, транспорта или общего абстрактного "объекта".
"""

STRUCTURE_EXTRACTION_SYSTEM = f"""
Ты извлекаешь из короткого текста устойчивое структурное представление для банковой кластеризации.

{DOMAIN_CONTEXT}

Нужно вернуть JSON с полями:
- is_meaningful
- polarity
- bank_area
- phenomenon
- object_name
- parent_focus
- parent_key
- specific_focus
- specific_key
- material_details
- context_details
- entities

Правила:
- Не используй заранее заданный справочник категорий.
- Формулируй поля только из смысла входного текста.
- Если текст короткий или бесполезный, верни false и "Не определено" в текстовых полях.
- specific_focus должен описывать наблюдаемую ситуацию, а не реакцию пользователя на нее.
- parent_focus должен быть шире, чем specific_focus.
- bank_area должен быть банковым/сервисным участком, а не абстрактным словом.
- object_name должен быть понятным банковым объектом или сущностью, если это возможно по тексту.
- context_details не должны менять specific_key, если без них кейс остается тем же.
- material_details должны менять specific_key только тогда, когда без них это уже другой кейс.
- Если комментарии отличаются только по реакции, срочности, эмоции или локальным обстоятельствам, их specific_key должен совпадать.

Пиши по-русски.
Без markdown.
Без пояснений вне JSON.
Только валидный JSON.
"""

STRUCTURE_EXTRACTION_HUMAN = """
Текст:
{text}

Дополнительный комментарий супервайзера:
{supervisor_feedback}

Верни JSON:
{{
  "is_meaningful": true,
  "polarity": "positive | negative | neutral | mixed",
  "bank_area": "...",
  "phenomenon": "...",
  "object_name": "...",
  "parent_focus": "...",
  "parent_key": "...",
  "specific_focus": "...",
  "specific_key": "...",
  "material_details": ["...", "..."],
  "context_details": ["...", "..."],
  "entities": ["...", "..."]
}}
"""

NEIGHBORHOOD_CRITIC_SYSTEM = f"""
Ты анализируешь локальный участок банковой кластеризации и ищешь локальные проблемы.

{DOMAIN_CONTEXT}

Твоя задача:
- понять, есть ли в этом участке дубли, слишком общие метки, нерелевантный домен, неверное родительство или смешанные кластеры
- если проблема есть, кратко описать, что именно нужно попробовать локально: merge, split, relabel или reassignment
- не предлагать глобальную перекластеризацию всего датасета

Верни только валидный JSON без markdown.
"""

NEIGHBORHOOD_CRITIC_HUMAN = """
Локальный участок:
{neighborhood_text}

Верни JSON:
{{
  "has_issue": true,
  "issue_summary": "...",
  "repair_instruction": "..."
}}
"""

PATCH_EVALUATION_SYSTEM = f"""
Ты сравниваешь два локальных варианта банковой кластеризации: до и после.

{DOMAIN_CONTEXT}

Нужно понять, стал ли локальный участок лучше.
Смотри на:
- меньше ли дублей
- лучше ли банковый контекст
- понятнее ли specific_group и parent_group
- не смешались ли разные кейсы
- не стало ли больше "Не определено" без причины

Верни только валидный JSON без markdown.
"""

PATCH_EVALUATION_HUMAN = """
До:
{before_text}

После:
{after_text}

Верни JSON:
{{
  "accepted": true,
  "summary": "..."
}}
"""
