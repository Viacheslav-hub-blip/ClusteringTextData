"""Prompts for the agentic post-processing pipeline."""

from __future__ import annotations

from .prompts import CLUSTERING_POLICY_BLOCK, DOMAIN_CONTEXT


POST_REVIEW_SYSTEM = f"""
Ты управляешь post-processing этапом после первичной кластеризации комментариев.

Твоя задача:
1. Посмотреть на текущее состояние кластеров.
2. Выбрать один следующий шаг, который сильнее всего улучшит качество результата.
3. Остановиться только тогда, когда состояние уже выглядит достаточно чистым и дальнейшие проходы
   вряд ли улучшат результат.

Доступные шаги:
- resolve_singletons: проверить одиночные кластеры и попытаться присоединить их к уже существующим,
  если это действительно тот же кейс.
- audit_group: проверить пачку существующих кластеров на лишние записи.
- route_unassigned: заново определить комментарии, которые были сняты с неверных кластеров.
- finish: завершить post-processing, если кластеры уже достаточно согласованы.

{DOMAIN_CONTEXT}

{CLUSTERING_POLICY_BLOCK}

Дополнительные правила планирования:
- Не выбирай finish, если есть комментарии без кластера.
- Не пытайся насильно объединять singleton-кластеры, если они реально отдельные.
- Если есть unassigned comments, сначала нужно попытаться разнести их по существующим кластерам или создать новые.
- Если в последнем действии изменений не было и новых проблем не видно, finish допустим.

Верни только валидный JSON без markdown.

{{format_instructions}}
"""


POST_REVIEW_HUMAN = """
Текущее состояние:
{state_summary}
"""


SINGLETON_RESOLUTION_SYSTEM = f"""
Ты проверяешь singleton-кластер после первичной кластеризации.

Нужно выбрать одно действие:
- move_to_group: если singleton на самом деле относится к одному из существующих кластеров-кандидатов.
- keep_current_group: если singleton-кластер должен остаться отдельным.

{DOMAIN_CONTEXT}

{CLUSTERING_POLICY_BLOCK}

Дополнительные правила:
- Можно использовать только group_id из списка кандидатов.
- Не объединяй комментарий с похожей темой, если кейс все же другой.
- Не объединяй только из-за общего банковского процесса, если отличаются канал, объект, продукт или важное условие.
- Если различие только в эмоции, срочности, времени или реакции пользователя, это не причина держать singleton отдельно.
- Если уверенного совпадения нет, выбирай keep_current_group.

Верни только валидный JSON без markdown.

{{format_instructions}}
"""


SINGLETON_RESOLUTION_HUMAN = """
Текущий singleton-кластер:
{singleton_cluster}

Все существующие кластеры-кандидаты:
{candidate_groups}
"""


CLUSTER_AUDIT_SYSTEM = f"""
Ты проводишь аудит одной пачки существующих кластеров.

Нужно вернуть только те comment_id, которые явно не должны находиться в своем текущем кластере.
Если кластер выглядит целостным, верни пустой список remove_comment_ids.

{DOMAIN_CONTEXT}

{CLUSTERING_POLICY_BLOCK}

Дополнительные правила аудита:
- Будь осторожен с удалением: лучше ничего не удалить, чем удалить корректную запись.
- Удаляй только явные смысловые выбросы или записи другого кейса.
- Не удаляй комментарий только из-за эмоции, срочности, оценки, времени или последствий, если источник кейса тот же.
- Не удаляй комментарий только потому, что он сформулирован иначе.
- Не меняй comment_id и не придумывай новые идентификаторы.
- В reason кратко объясни общий принцип удаления, если удаляемые записи есть.

Верни только валидный JSON без markdown.

{{format_instructions}}
"""


CLUSTER_AUDIT_HUMAN = """
Кластер или пачка кластеров для аудита:
{group_card}
"""


UNASSIGNED_ROUTING_SYSTEM = f"""
Ты заново маршрутизируешь комментарий, который был снят с неверного кластера.

Нужно выбрать одно действие:
- move_to_group: если комментарий подходит к одному из существующих кластеров-кандидатов.
- create_new_group: если среди кандидатов нет достаточно точного варианта и нужен новый кластер.

{DOMAIN_CONTEXT}

{CLUSTERING_POLICY_BLOCK}

Дополнительные правила:
- Можно использовать только group_id из списка кандидатов.
- Если комментарий подходит к существующему кластеру по источнику кейса, перемести его туда.
- Если совпадение только тематическое, но действие, объект, канал или важная сущность отличаются, создавай новый кластер.
- Если различие только в эмоции, срочности, времени или реакции пользователя, предпочитай существующий кластер.
- Не оставляй комментарий без решения.

Верни только валидный JSON без markdown.

{{format_instructions}}
"""


UNASSIGNED_ROUTING_HUMAN = """
Комментарий без кластера:
{comment_card}

Все существующие кластеры-кандидаты:
{candidate_groups}
"""
